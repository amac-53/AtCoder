WITH ranked_data AS (
    SELECT
        ROW_NUMBER() OVER(ORDER BY score DESC, id ASC) "順位",
        name AS "名前",
        score AS "スコア"
    FROM
        results
    GROUP BY
        name
)

SELECT
    *
FROM
    ranked_dta
WHERE
    `順位` IN (1, 2, 3, 10);