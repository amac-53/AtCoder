WITH subject_data AS (
    SELECT email, game_id, score FROM results
    EXCEPT
    SELECT email, game_id, score FROM optout
)

SELECT
    email
FROM
    subject_data
GROUP BY
    email
ORDER BY
    SUM(score) DESC
LIMIT 10;
